services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: next-portfolio
    restart: unless-stopped
    ports:
      - "127.0.0.1:3001:3000"
    environment:
      NODE_ENV: production
